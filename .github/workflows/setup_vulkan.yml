name: build_and_test
on:
  #push:
    #branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  BUILD_TYPE: 
  VULKAN_VERSION: 1.3.204.0

jobs:
  build_and_run:
    runs_on: macos-latest
    steps:
      -uses: actions/checkout@v3
      -uses: ./.github/actions
        with:
          vulkan_version: ${VULKAN_VERSION}
          vulkan_components: Vulkan-Headers, Vulkan-Loader, Glslang
      -name: install vulkan
        run: 